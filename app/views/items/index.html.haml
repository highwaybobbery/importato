%h1 Listing items

%table
  %thead
    %tr
      %th Description
      %th Price
      %th Merchant

  %tbody
    - @items.each do |item|
      %tr
        %td= item.description
        %td= item.price
        %td= item.merchant
